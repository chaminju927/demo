<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
  
<configuration>
<properties resource="appliation.properties" />
  <typeAliases>
    <typeAlias type="demo.example.Worker.Vo.Worker" alias="workerMap" />    
  </typeAliases>
  
   <environments default="development">
         <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="UNPOOLED">
                <property name="name" value="${name}"/>
                <property name="no" value="${no}"/>
                <property name="email" value="${email}"/>
                <property name="phone" value="${phone}"/>
            </dataSource>
        </environment>
    </environments>
 
    <mappers>
        <mapper resource="mybatis/mapper/WorkerDao.xml"/>
    </mappers>

</configuration>